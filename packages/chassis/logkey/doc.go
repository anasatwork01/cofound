// Package logkey is the closed vocabulary of structured log field names.
//
// The vocabulary itself is generated, because the Python chassis needs the same
// field names and a retyped copy would drift. See keys.gen.go and
// packages/schema/observability.json.
package logkey
