// Package tools exists only so this module has a package for gofmt, go vet and
// go test to walk. The generators themselves are declared as `tool` directives
// in go.mod and invoked with `go tool`.
package tools
