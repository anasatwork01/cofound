// Package apiv1 provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.8.0 DO NOT EDIT.
package apiv1

import (
	"encoding/json"

	externalRef0 "github.com/anasatwork01/cofound/packages/schema/gen/go/common"
	"github.com/oapi-codegen/runtime"
	openapi_types "github.com/oapi-codegen/runtime/types"
)

// Defines values for GitAuthority.
const (
	Github   GitAuthority = "github"
	Internal GitAuthority = "internal"
)

// Valid indicates whether the value is a known member of the GitAuthority enum.
func (e GitAuthority) Valid() bool {
	switch e {
	case Github:
		return true
	case Internal:
		return true
	default:
		return false
	}
}

// AuthOrgMembership One org the signed-in user belongs to, and their role in it.
type AuthOrgMembership struct {
	Id   externalRef0.Uuid `json:"id"`
	Name string            `json:"name"`

	// Role SPEC 8. Approving anything that spends money requires owner or admin.
	Role externalRef0.Role `json:"role"`

	// Slug Lowercase, DNS-label safe: a project slug reaches a preview hostname (SPEC 9).
	Slug externalRef0.Slug `json:"slug"`
}

// AuthSession The signed-in user. Named AuthSession rather than Session because
// `Session` already means an AGENT session here — a project, a branch, a
// sandbox (SPEC 6) — and the two are unrelated. The same collision is why
// migration 00014 calls its table `user_sessions`.
//
// Deliberately NOT the session token or its id: the console needs neither,
// and a body carrying one would put a credential into anything that
// logged a response.
type AuthSession struct {
	// Orgs Every org the user belongs to, with their role in each. SPEC 8:
	// users may belong to many orgs, and the project picker switches
	// between them — so the console needs the whole list on load rather
	// than one request per org.
	Orgs []AuthOrgMembership `json:"orgs"`
	User AuthUser            `json:"user"`
}

// AuthUser defines model for AuthUser.
type AuthUser struct {
	Email openapi_types.Email `json:"email"`
	Id    externalRef0.Uuid   `json:"id"`
	Name  *string             `json:"name,omitempty"`
}

// Branch defines model for Branch.
type Branch struct {
	// HeadSha Full or abbreviated. SPEC 18 demotes these in the UI but never hides them.
	HeadSha *externalRef0.GitSha `json:"head_sha,omitempty"`
	Id      externalRef0.Uuid    `json:"id"`
	Name    string               `json:"name"`
}

// CreateInviteRequest defines model for CreateInviteRequest.
type CreateInviteRequest struct {
	Email openapi_types.Email `json:"email"`

	// Role SPEC 8. Approving anything that spends money requires owner or admin.
	Role externalRef0.Role `json:"role"`
}

// CreateOrgRequest defines model for CreateOrgRequest.
type CreateOrgRequest struct {
	Name string `json:"name"`

	// Slug Lowercase, DNS-label safe: a project slug reaches a preview hostname (SPEC 9).
	Slug *externalRef0.Slug `json:"slug,omitempty"`
}

// CreateProjectRequest Exactly one of `template_version_id` or `prompt` (SPEC 7.1).
type CreateProjectRequest struct {
	union json.RawMessage
}

// CreateProjectRequest0 defines model for CreateProjectRequest.0.
type CreateProjectRequest0 struct {
	Name              string            `json:"name"`
	OrgId             externalRef0.Uuid `json:"org_id"`
	TemplateVersionId externalRef0.Uuid `json:"template_version_id"`
}

// CreateProjectRequest1 defines model for CreateProjectRequest.1.
type CreateProjectRequest1 struct {
	Name   *string           `json:"name,omitempty"`
	OrgId  externalRef0.Uuid `json:"org_id"`
	Prompt string            `json:"prompt"`
}

// CreateSessionRequest defines model for CreateSessionRequest.
type CreateSessionRequest struct {
	Branch string `json:"branch"`
}

// CreateTurnRequest defines model for CreateTurnRequest.
type CreateTurnRequest struct {
	Prompt string `json:"prompt"`
}

// GitAuthority SPEC 5.5: exactly one owner per project at all times. Once GitHub is
// linked it becomes authoritative and the internal store is a cache plus
// outbound queue. Dual authority on one branch is a split-brain bug
// factory; do not build it.
type GitAuthority string

// Invite defines model for Invite.
type Invite struct {
	Email openapi_types.Email `json:"email"`

	// ExpiresAt RFC 3339, always UTC. Ads reporting is timezone-bound per account (SPEC 19.4) and converts at the edge of the system, never in storage.
	ExpiresAt externalRef0.Timestamp `json:"expires_at"`
	Id        externalRef0.Uuid      `json:"id"`

	// Role SPEC 8. Approving anything that spends money requires owner or admin.
	Role externalRef0.Role `json:"role"`
}

// Lease SPEC 5.4 keeps this behind an interface so Redis can be swapped for Durable Objects in phase 6.
type Lease struct {
	// ExpiresAt RFC 3339, always UTC. Ads reporting is timezone-bound per account (SPEC 19.4) and converts at the edge of the system, never in storage.
	ExpiresAt     *externalRef0.Timestamp `json:"expires_at,omitempty"`
	Holder        LeaseHolder             `json:"holder"`
	PendingHolder *LeaseHolder            `json:"pending_holder,omitempty"`
}

// LeaseHolder defines model for LeaseHolder.
type LeaseHolder = interface{}

// LeaseRequest defines model for LeaseRequest.
type LeaseRequest struct {
	Action interface{} `json:"action"`
}

// Org defines model for Org.
type Org struct {
	// CreatedAt RFC 3339, always UTC. Ads reporting is timezone-bound per account (SPEC 19.4) and converts at the edge of the system, never in storage.
	CreatedAt externalRef0.Timestamp `json:"created_at"`
	Id        externalRef0.Uuid      `json:"id"`
	Name      string                 `json:"name"`
	Plan      string                 `json:"plan"`

	// Slug Lowercase, DNS-label safe: a project slug reaches a preview hostname (SPEC 9).
	Slug externalRef0.Slug `json:"slug"`
}

// OrgMember defines model for OrgMember.
type OrgMember struct {
	Email openapi_types.Email `json:"email"`
	Name  *string             `json:"name,omitempty"`

	// Role SPEC 8. Approving anything that spends money requires owner or admin.
	Role   externalRef0.Role `json:"role"`
	UserId externalRef0.Uuid `json:"user_id"`
}

// Project defines model for Project.
type Project struct {
	// ArchivedAt RFC 3339, always UTC. Ads reporting is timezone-bound per account (SPEC 19.4) and converts at the edge of the system, never in storage.
	ArchivedAt *externalRef0.Timestamp `json:"archived_at,omitempty"`
	Branches   *[]Branch               `json:"branches,omitempty"`

	// CreatedAt RFC 3339, always UTC. Ads reporting is timezone-bound per account (SPEC 19.4) and converts at the edge of the system, never in storage.
	CreatedAt     externalRef0.Timestamp `json:"created_at"`
	DefaultBranch string                 `json:"default_branch"`

	// GitAuthority SPEC 5.5: exactly one owner per project at all times. Once GitHub is
	// linked it becomes authoritative and the internal store is a cache plus
	// outbound queue. Dual authority on one branch is a split-brain bug
	// factory; do not build it.
	GitAuthority GitAuthority      `json:"git_authority"`
	Id           externalRef0.Uuid `json:"id"`
	Name         string            `json:"name"`
	OrgId        externalRef0.Uuid `json:"org_id"`

	// Slug Lowercase, DNS-label safe: a project slug reaches a preview hostname (SPEC 9).
	Slug externalRef0.Slug `json:"slug"`
}

// Template defines model for Template.
type Template struct {
	CurrentVersion TemplateVersion `json:"current_version"`
	DisplayName    string          `json:"display_name"`
	Id             string          `json:"id"`
	Summary        *string         `json:"summary,omitempty"`
}

// TemplateVersion defines model for TemplateVersion.
type TemplateVersion struct {
	Id externalRef0.Uuid `json:"id"`

	// ImageReady Whether the pre-baked sandbox image exists. Without it, project creation misses the p50 < 10s SLO (SPEC 9, 17.3).
	ImageReady bool   `json:"image_ready"`
	Version    string `json:"version"`
}

// Turn defines model for Turn.
type Turn struct {
	Actor TurnActor `json:"actor"`

	// CheckpointSha Full or abbreviated. SPEC 18 demotes these in the UI but never hides them.
	CheckpointSha *externalRef0.GitSha `json:"checkpoint_sha,omitempty"`

	// CreditsCharged A decimal string, not a number. The control plane stores credits as numeric(14,4) (SPEC 6) and SPEC 16.1 requires a ledger rather than a counter; routing money through an IEEE-754 double loses that exactness silently. Negative values are legal: SPEC 16.1 allows a bounded negative float rather than killing a running turn.
	CreditsCharged *externalRef0.Credits `json:"credits_charged,omitempty"`

	// EndedAt RFC 3339, always UTC. Ads reporting is timezone-bound per account (SPEC 19.4) and converts at the edge of the system, never in storage.
	EndedAt *externalRef0.Timestamp `json:"ended_at,omitempty"`
	Id      externalRef0.Uuid       `json:"id"`
	Seq     int                     `json:"seq"`

	// StartedAt RFC 3339, always UTC. Ads reporting is timezone-bound per account (SPEC 19.4) and converts at the edge of the system, never in storage.
	StartedAt externalRef0.Timestamp `json:"started_at"`

	// Status Mirrors turns.status in SPEC 6.
	Status TurnStatus `json:"status"`
}

// TurnActor defines model for TurnActor.
type TurnActor = interface{}

// TurnStatus Mirrors turns.status in SPEC 6.
type TurnStatus = interface{}

// IdempotencyKey defines model for IdempotencyKey.
type IdempotencyKey = string

// OrgSlugParam Lowercase, DNS-label safe: a project slug reaches a preview hostname (SPEC 9).
type OrgSlugParam = externalRef0.Slug

// OrgContextHeader Lowercase, DNS-label safe: a project slug reaches a preview hostname (SPEC 9).
type OrgContextHeader = externalRef0.Slug

// ProjectSlugParam Lowercase, DNS-label safe: a project slug reaches a preview hostname (SPEC 9).
type ProjectSlugParam = externalRef0.Slug

// SessionIdParam defines model for Session.
type SessionIdParam = externalRef0.Uuid

// Conflict defines model for Conflict.
type Conflict = externalRef0.ErrorResponse

// Forbidden defines model for Forbidden.
type Forbidden = externalRef0.ErrorResponse

// Invalid defines model for Invalid.
type Invalid = externalRef0.ErrorResponse

// NotFound defines model for NotFound.
type NotFound = externalRef0.ErrorResponse

// PaymentRequired defines model for PaymentRequired.
type PaymentRequired = externalRef0.ErrorResponse

// RateLimited defines model for RateLimited.
type RateLimited = externalRef0.ErrorResponse

// Unauthenticated defines model for Unauthenticated.
type Unauthenticated = externalRef0.ErrorResponse

// CompleteGoogleSignInParams defines parameters for CompleteGoogleSignIn.
type CompleteGoogleSignInParams struct {
	Code  *string `form:"code,omitempty" json:"code,omitempty"`
	State *string `form:"state,omitempty" json:"state,omitempty"`

	// Error Set by Google when the user declined.
	Error *string `form:"error,omitempty" json:"error,omitempty"`
}

// RequestMagicLinkJSONBody defines parameters for RequestMagicLink.
type RequestMagicLinkJSONBody struct {
	Email openapi_types.Email `json:"email"`
}

// VerifyMagicLinkJSONBody defines parameters for VerifyMagicLink.
type VerifyMagicLinkJSONBody struct {
	Token string `json:"token"`
}

// AcceptInviteJSONBody defines parameters for AcceptInvite.
type AcceptInviteJSONBody struct {
	Token string `json:"token"`
}

// AcceptInviteParams defines parameters for AcceptInvite.
type AcceptInviteParams struct {
	// IdempotencyKey Replaying a key returns the original response instead of acting twice (SPEC 7.1, 17.2).
	IdempotencyKey *IdempotencyKey `json:"Idempotency-Key,omitempty"`
}

// CreateOrgParams defines parameters for CreateOrg.
type CreateOrgParams struct {
	// IdempotencyKey Replaying a key returns the original response instead of acting twice (SPEC 7.1, 17.2).
	IdempotencyKey *IdempotencyKey `json:"Idempotency-Key,omitempty"`
}

// CreateInviteParams defines parameters for CreateInvite.
type CreateInviteParams struct {
	// IdempotencyKey Replaying a key returns the original response instead of acting twice (SPEC 7.1, 17.2).
	IdempotencyKey *IdempotencyKey `json:"Idempotency-Key,omitempty"`
}

// RemoveOrgMemberParams defines parameters for RemoveOrgMember.
type RemoveOrgMemberParams struct {
	// IdempotencyKey Replaying a key returns the original response instead of acting twice (SPEC 7.1, 17.2).
	IdempotencyKey *IdempotencyKey `json:"Idempotency-Key,omitempty"`
}

// ChangeMemberRoleJSONBody defines parameters for ChangeMemberRole.
type ChangeMemberRoleJSONBody struct {
	// Role SPEC 8. Approving anything that spends money requires owner or admin.
	Role externalRef0.Role `json:"role"`
}

// ChangeMemberRoleParams defines parameters for ChangeMemberRole.
type ChangeMemberRoleParams struct {
	// IdempotencyKey Replaying a key returns the original response instead of acting twice (SPEC 7.1, 17.2).
	IdempotencyKey *IdempotencyKey `json:"Idempotency-Key,omitempty"`
}

// ListProjectsParams defines parameters for ListProjects.
type ListProjectsParams struct {
	// OrgContextHeader Which organisation the request is about, by slug.
	//
	// SPEC 7.1's project paths take a project SLUG, and SPEC 6 makes a project
	// slug unique only WITHIN an org (`unique (org_id, slug)`). So
	// `/v1/projects/{project}` is ambiguous for a user who belongs to two orgs
	// that each have a project of that name — and 7.1 defines no way to say
	// which, even though SPEC 8 requires "org switching in the project
	// picker", which means the console has a current org to send.
	//
	// Optional, so single-org callers stay on the path 7.1 specifies. A
	// request without it resolves across the caller's memberships and is
	// refused with `ambiguous_project` (409) only if genuinely ambiguous —
	// never served against a guess, because that would mean acting on the
	// wrong tenant's project. It also selects the org for routes that name
	// none, such as creating a project.
	//
	// See docs/open-questions.md Q6.
	OrgContextHeader *OrgContextHeader `json:"X-Halyard-Org,omitempty"`
}

// CreateProjectParams defines parameters for CreateProject.
type CreateProjectParams struct {
	// IdempotencyKey Replaying a key returns the original response instead of acting twice (SPEC 7.1, 17.2).
	IdempotencyKey *IdempotencyKey `json:"Idempotency-Key,omitempty"`

	// OrgContextHeader Which organisation the request is about, by slug.
	//
	// SPEC 7.1's project paths take a project SLUG, and SPEC 6 makes a project
	// slug unique only WITHIN an org (`unique (org_id, slug)`). So
	// `/v1/projects/{project}` is ambiguous for a user who belongs to two orgs
	// that each have a project of that name — and 7.1 defines no way to say
	// which, even though SPEC 8 requires "org switching in the project
	// picker", which means the console has a current org to send.
	//
	// Optional, so single-org callers stay on the path 7.1 specifies. A
	// request without it resolves across the caller's memberships and is
	// refused with `ambiguous_project` (409) only if genuinely ambiguous —
	// never served against a guess, because that would mean acting on the
	// wrong tenant's project. It also selects the org for routes that name
	// none, such as creating a project.
	//
	// See docs/open-questions.md Q6.
	OrgContextHeader *OrgContextHeader `json:"X-Halyard-Org,omitempty"`
}

// DeleteProjectParams defines parameters for DeleteProject.
type DeleteProjectParams struct {
	// IdempotencyKey Replaying a key returns the original response instead of acting twice (SPEC 7.1, 17.2).
	IdempotencyKey *IdempotencyKey `json:"Idempotency-Key,omitempty"`

	// OrgContextHeader Which organisation the request is about, by slug.
	//
	// SPEC 7.1's project paths take a project SLUG, and SPEC 6 makes a project
	// slug unique only WITHIN an org (`unique (org_id, slug)`). So
	// `/v1/projects/{project}` is ambiguous for a user who belongs to two orgs
	// that each have a project of that name — and 7.1 defines no way to say
	// which, even though SPEC 8 requires "org switching in the project
	// picker", which means the console has a current org to send.
	//
	// Optional, so single-org callers stay on the path 7.1 specifies. A
	// request without it resolves across the caller's memberships and is
	// refused with `ambiguous_project` (409) only if genuinely ambiguous —
	// never served against a guess, because that would mean acting on the
	// wrong tenant's project. It also selects the org for routes that name
	// none, such as creating a project.
	//
	// See docs/open-questions.md Q6.
	OrgContextHeader *OrgContextHeader `json:"X-Halyard-Org,omitempty"`
}

// GetProjectParams defines parameters for GetProject.
type GetProjectParams struct {
	// OrgContextHeader Which organisation the request is about, by slug.
	//
	// SPEC 7.1's project paths take a project SLUG, and SPEC 6 makes a project
	// slug unique only WITHIN an org (`unique (org_id, slug)`). So
	// `/v1/projects/{project}` is ambiguous for a user who belongs to two orgs
	// that each have a project of that name — and 7.1 defines no way to say
	// which, even though SPEC 8 requires "org switching in the project
	// picker", which means the console has a current org to send.
	//
	// Optional, so single-org callers stay on the path 7.1 specifies. A
	// request without it resolves across the caller's memberships and is
	// refused with `ambiguous_project` (409) only if genuinely ambiguous —
	// never served against a guess, because that would mean acting on the
	// wrong tenant's project. It also selects the org for routes that name
	// none, such as creating a project.
	//
	// See docs/open-questions.md Q6.
	OrgContextHeader *OrgContextHeader `json:"X-Halyard-Org,omitempty"`
}

// CreateSessionParams defines parameters for CreateSession.
type CreateSessionParams struct {
	// IdempotencyKey Replaying a key returns the original response instead of acting twice (SPEC 7.1, 17.2).
	IdempotencyKey *IdempotencyKey `json:"Idempotency-Key,omitempty"`
}

// AbortTurnParams defines parameters for AbortTurn.
type AbortTurnParams struct {
	// IdempotencyKey Replaying a key returns the original response instead of acting twice (SPEC 7.1, 17.2).
	IdempotencyKey *IdempotencyKey `json:"Idempotency-Key,omitempty"`
}

// StreamSessionEventsParams defines parameters for StreamSessionEvents.
type StreamSessionEventsParams struct {
	// LastEventID Resume point from a dropped connection.
	LastEventID *string `json:"Last-Event-ID,omitempty"`
}

// RequestLeaseParams defines parameters for RequestLease.
type RequestLeaseParams struct {
	// IdempotencyKey Replaying a key returns the original response instead of acting twice (SPEC 7.1, 17.2).
	IdempotencyKey *IdempotencyKey `json:"Idempotency-Key,omitempty"`
}

// CreateTurnParams defines parameters for CreateTurn.
type CreateTurnParams struct {
	// IdempotencyKey Replaying a key returns the original response instead of acting twice (SPEC 7.1, 17.2).
	IdempotencyKey *IdempotencyKey `json:"Idempotency-Key,omitempty"`
}

// RequestMagicLinkJSONRequestBody defines body for RequestMagicLink for application/json ContentType.
type RequestMagicLinkJSONRequestBody RequestMagicLinkJSONBody

// VerifyMagicLinkJSONRequestBody defines body for VerifyMagicLink for application/json ContentType.
type VerifyMagicLinkJSONRequestBody VerifyMagicLinkJSONBody

// AcceptInviteJSONRequestBody defines body for AcceptInvite for application/json ContentType.
type AcceptInviteJSONRequestBody AcceptInviteJSONBody

// CreateOrgJSONRequestBody defines body for CreateOrg for application/json ContentType.
type CreateOrgJSONRequestBody = CreateOrgRequest

// CreateInviteJSONRequestBody defines body for CreateInvite for application/json ContentType.
type CreateInviteJSONRequestBody = CreateInviteRequest

// ChangeMemberRoleJSONRequestBody defines body for ChangeMemberRole for application/json ContentType.
type ChangeMemberRoleJSONRequestBody ChangeMemberRoleJSONBody

// CreateProjectJSONRequestBody defines body for CreateProject for application/json ContentType.
type CreateProjectJSONRequestBody = CreateProjectRequest

// CreateSessionJSONRequestBody defines body for CreateSession for application/json ContentType.
type CreateSessionJSONRequestBody = CreateSessionRequest

// RequestLeaseJSONRequestBody defines body for RequestLease for application/json ContentType.
type RequestLeaseJSONRequestBody = LeaseRequest

// CreateTurnJSONRequestBody defines body for CreateTurn for application/json ContentType.
type CreateTurnJSONRequestBody = CreateTurnRequest

// AsCreateProjectRequest0 returns the union data inside the CreateProjectRequest as a CreateProjectRequest0
func (t CreateProjectRequest) AsCreateProjectRequest0() (CreateProjectRequest0, error) {
	var body CreateProjectRequest0
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromCreateProjectRequest0 overwrites any union data inside the CreateProjectRequest as the provided CreateProjectRequest0
func (t *CreateProjectRequest) FromCreateProjectRequest0(v CreateProjectRequest0) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeCreateProjectRequest0 performs a merge with any union data inside the CreateProjectRequest, using the provided CreateProjectRequest0
func (t *CreateProjectRequest) MergeCreateProjectRequest0(v CreateProjectRequest0) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

// AsCreateProjectRequest1 returns the union data inside the CreateProjectRequest as a CreateProjectRequest1
func (t CreateProjectRequest) AsCreateProjectRequest1() (CreateProjectRequest1, error) {
	var body CreateProjectRequest1
	err := json.Unmarshal(t.union, &body)
	return body, err
}

// FromCreateProjectRequest1 overwrites any union data inside the CreateProjectRequest as the provided CreateProjectRequest1
func (t *CreateProjectRequest) FromCreateProjectRequest1(v CreateProjectRequest1) error {
	b, err := json.Marshal(v)
	t.union = b
	return err
}

// MergeCreateProjectRequest1 performs a merge with any union data inside the CreateProjectRequest, using the provided CreateProjectRequest1
func (t *CreateProjectRequest) MergeCreateProjectRequest1(v CreateProjectRequest1) error {
	b, err := json.Marshal(v)
	if err != nil {
		return err
	}

	merged, err := runtime.JSONMerge(t.union, b)
	t.union = merged
	return err
}

func (t CreateProjectRequest) MarshalJSON() ([]byte, error) {
	b, err := t.union.MarshalJSON()
	return b, err
}

func (t *CreateProjectRequest) UnmarshalJSON(b []byte) error {
	err := t.union.UnmarshalJSON(b)
	return err
}
