// Package sandboxdapi provides primitives to interact with the openapi HTTP API.
//
// Code generated by github.com/oapi-codegen/oapi-codegen/v2 version v2.8.0 DO NOT EDIT.
package sandboxdapi

import (
	externalRef0 "github.com/anasatwork01/cofound/packages/schema/gen/go/common"
)

// CreateSessionRequest defines model for CreateSessionRequest.
type CreateSessionRequest struct {
	Branch string `json:"branch"`

	// HeadSha Full or abbreviated. SPEC 18 demotes these in the UI but never hides them.
	HeadSha   *externalRef0.GitSha `json:"head_sha,omitempty"`
	OrgId     externalRef0.Uuid    `json:"org_id"`
	ProjectId externalRef0.Uuid    `json:"project_id"`

	// Quotas SPEC 9. Hard limits, enforced in agentd and again in aigw — the sandbox
	// is untrusted, so a limit checked only inside it is not a limit.
	// Omitted fields take the platform default.
	Quotas *SessionQuotas `json:"quotas,omitempty"`

	// TemplateImage Pre-baked image for the project's template version (SPEC 9 step 2).
	TemplateImage string `json:"template_image"`
}

// Health defines model for Health.
type Health struct {
	Status  interface{} `json:"status"`
	Version *string     `json:"version,omitempty"`
}

// Session defines model for Session.
type Session struct {
	// SandboxId Modal's identifier. Opaque to the control plane.
	SandboxId *string           `json:"sandbox_id,omitempty"`
	SessionId externalRef0.Uuid `json:"session_id"`

	// StartedAt RFC 3339, always UTC. Ads reporting is timezone-bound per account (SPEC 19.4) and converts at the edge of the system, never in storage.
	StartedAt externalRef0.Timestamp `json:"started_at"`

	// State Mirrors sessions.state in SPEC 6.
	State SessionState `json:"state"`

	// TunnelUrl Registered against <branch>.<project>.preview.<domain> in Workers KV
	// (SPEC 9 step 5). Null until the dev server is listening. Never
	// handed to a browser unsigned — SPEC 14.3 gates preview URLs behind
	// a signed cookie, because an unlisted URL is not access control.
	TunnelUrl *string `json:"tunnel_url,omitempty"`

	// Warm Whether this came from the warm pool. The single biggest lever on perceived quality (SPEC 9), so it is measured.
	Warm bool `json:"warm"`
}

// SessionQuotas SPEC 9. Hard limits, enforced in agentd and again in aigw — the sandbox
// is untrusted, so a limit checked only inside it is not a limit.
// Omitted fields take the platform default.
type SessionQuotas struct {
	MemoryMb                *int     `json:"memory_mb,omitempty"`
	TokensPerSession        *int     `json:"tokens_per_session,omitempty"`
	TokensPerTurn           *int     `json:"tokens_per_turn,omitempty"`
	Vcpu                    *float32 `json:"vcpu,omitempty"`
	WallClockSecondsPerTurn *int     `json:"wall_clock_seconds_per_turn,omitempty"`
}

// SessionState Mirrors sessions.state in SPEC 6.
type SessionState = interface{}

// CreateSessionJSONRequestBody defines body for CreateSession for application/json ContentType.
type CreateSessionJSONRequestBody = CreateSessionRequest
