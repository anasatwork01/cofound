/* eslint-disable */
/**
 * This file was automatically generated by json-schema-to-typescript.
 * DO NOT MODIFY IT BY HAND. Instead, modify the source JSONSchema file,
 * and run json-schema-to-typescript to regenerate this file.
 */

/**
 * The agent event stream defined in SPEC 7.2. agentd emits these and api relays them over SSE with a monotonic id for Last-Event-ID resume.
 *
 * Two rules this schema cannot express, which every decoder must honour:
 *   1. Unknown `type` values are ignored, not fatal (SPEC 7.2, 18). Objects      therefore permit additional properties: a newer producer must not      break an older consumer. Decode the envelope first, then the variant.
 *   2. usage.tick is advisory. Billing reads usage_events, never this stream.
 */
export type AgentEvent =
  | TurnStarted
  | MessageDelta
  | ToolStarted
  | ToolFinished
  | LeaseChanged
  | PreviewStatus
  | UsageTick
  | TurnFinished
  | ErrorEvent;
export type TurnStartedType = "turn.started";
/**
 * A decimal string, not a number. The control plane stores credits as numeric(14,4) (SPEC 6) and SPEC 16.1 requires a ledger rather than a counter; routing money through an IEEE-754 double loses that exactness silently. Negative values are legal: SPEC 16.1 allows a bounded negative float rather than killing a running turn.
 */
export type Credits = string;
export type MessageDeltaType = "message.delta";
export type ToolStartedType = "tool.started";
export type ToolFinishedType = "tool.finished";
export type LeaseChangedType = "lease.changed";
export type LeaseHolder = "agent" | "human" | "none";
export type PreviewStatusType = "preview.status";
export type PreviewState = "starting" | "rebuilding" | "ready" | "failed";
export type UsageTickType = "usage.tick";
export type TurnFinishedType = "turn.finished";
export type TurnStatus = "done" | "failed" | "aborted" | "budget_exceeded";
/**
 * Full or abbreviated. SPEC 18 demotes these in the UI but never hides them.
 */
export type GitSha = string;
export type ErrorEventType = "error";

/**
 * SPEC 16.1 reserve-then-settle: the hold is placed before work starts, at the p75 estimate.
 */
export interface TurnStarted {
  type: TurnStartedType;
  /**
   * SPEC 7.2: every event carries the turn it belongs to.
   */
  turn: number;
  hold: Hold;
}
export interface Hold {
  credits: Credits;
}
/**
 * An incremental chunk of assistant prose. Concatenate in `id` order; SPEC 18 renders these optimistically.
 */
export interface MessageDelta {
  type: MessageDeltaType;
  /**
   * SPEC 7.2: every event carries the turn it belongs to.
   */
  turn: number;
  text: string;
}
/**
 * SPEC 18 appends these to the current turn card as they arrive.
 */
export interface ToolStarted {
  type: ToolStartedType;
  /**
   * SPEC 7.2: every event carries the turn it belongs to.
   */
  turn: number;
  /**
   * Correlates with the matching tool.finished.
   */
  id: string;
  tool: string;
  /**
   * What the tool acts on — a repo-relative path for file tools.
   */
  target?: string;
}
/**
 * diff_stat is present only for tools that changed the working tree.
 */
export interface ToolFinished {
  type: ToolFinishedType;
  /**
   * SPEC 7.2: every event carries the turn it belongs to.
   */
  turn: number;
  id: string;
  ok: boolean;
  summary?: string;
  diff_stat?: DiffStat;
}
export interface DiffStat {
  add: number;
  del: number;
}
/**
 * SPEC 18: when the agent holds the lease the editor goes read-only with a visible banner, never a silently rejecting editor.
 */
export interface LeaseChanged {
  type: LeaseChangedType;
  /**
   * SPEC 7.2: every event carries the turn it belongs to.
   */
  turn: number;
  holder: LeaseHolder;
}
/**
 * Preview lifecycle. SPEC 17.3 tracks rebuild-after-edit at p95 < 8s.
 */
export interface PreviewStatus {
  type: PreviewStatusType;
  /**
   * SPEC 7.2: every event carries the turn it belongs to.
   */
  turn: number;
  state: PreviewState;
  /**
   * Signed preview URL. SPEC 14.3: an unlisted URL is not access control.
   */
  url?: string;
}
/**
 * SPEC 7.2: advisory for the UI, authoritative only once reconciled server-side. Never bill from this event.
 */
export interface UsageTick {
  type: UsageTickType;
  /**
   * SPEC 7.2: every event carries the turn it belongs to.
   */
  turn: number;
  tokens: TokenUsage;
  sandbox_seconds: number;
}
/**
 * Four separate counts, never one total. SPEC 16.2: they differ by roughly an order of magnitude in cost, and a combined meter misprices heavy sessions. The upstream agent reports FIVE fields and two of them are already net of something, so each field below states exactly what it receives - the obvious mapping under-bills. See docs/verified.md, SPEC 22 item 6, 'The metering trap'.
 */
export interface TokenUsage {
  /**
   * The agent's already cache-adjusted input count, NOT the provider's raw input. Upstream computes it as inputTokens - cacheRead - cacheWrite, so total billable input is in + cache_read + cache_write. Putting the provider's raw count here double-counts the cache fields.
   */
  in: number;
  /**
   * Output PLUS reasoning. Upstream's output field already excludes reasoning tokens, and SPEC 16.2 mandates four meters with no fifth, so reasoning is folded in here. Emitting upstream's output verbatim never bills reasoning at all.
   */
  out: number;
  /**
   * Cache-read input tokens, counted separately because they are roughly an order of magnitude cheaper. Billable, and on a cached agent loop usually the majority of input.
   */
  cache_read: number;
  /**
   * Cache-write input tokens. Billable, and normalised upstream across providers that report it only in metadata.
   */
  cache_write: number;
}
/**
 * Mirrors turns.status in SPEC 6. checkpoint_sha is absent when the turn changed no files.
 */
export interface TurnFinished {
  type: TurnFinishedType;
  /**
   * SPEC 7.2: every event carries the turn it belongs to.
   */
  turn: number;
  status: TurnStatus;
  checkpoint_sha?: GitSha;
  credits?: Credits;
}
/**
 * Turn-scoped failure. Transport-level failures surface as an SSE disconnect instead.
 */
export interface ErrorEvent {
  type: ErrorEventType;
  /**
   * SPEC 7.2: every event carries the turn it belongs to.
   */
  turn: number;
  code: string;
  message: string;
  retriable: boolean;
}
