# packages/ui

Shared React components for the console. Tokens come from the approved mockup
(`docs/mockup.html`) - extract them, do not invent new ones (SPEC §3.1).

Semantic colour has exactly three states (SPEC §18):
violet = agent-owned action, amber = waiting on you, teal = live.
