[project]
name = "halyard-workers"
version = "0.0.0"
description = "Ads sync, SEO crawl and audit, rating, reconciliation, GitHub sync"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/halyard_workers"]
